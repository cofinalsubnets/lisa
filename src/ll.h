#ifndef _ll_h
#define _ll_h
#include <stdint.h>
struct carrier;
void li_fin(struct carrier*);
enum status
  eval(struct carrier*, intptr_t),
  li_ini(struct carrier*),
  li_go(struct carrier*);
#endif
